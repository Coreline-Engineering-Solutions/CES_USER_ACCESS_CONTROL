export const environment = {
  production: false,
  // GIS API — hosts /roles/* (this app) and /stock/* (CES_STOCK_MANAGER).
  // Per STOCK_ROLES_API_HANDOVER.md (2026-07-26).
  apiBaseUrl: 'https://gis-api.onrender.com'
};
